name: Build

on:
  push:
  pull_request:

jobs:
  macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build Pomote
        run: ./build-app.sh
      - name: Verify bundle
        run: codesign --verify --deep --strict dist/Pomote.app
